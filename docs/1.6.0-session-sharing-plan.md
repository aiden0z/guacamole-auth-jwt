# Session and sharing implementation plan

Spec: [approved design](1.6.0-session-sharing-design.md). Execute in this session; do not publish.

Constraints: Guacamole 1.6.0; Java 8 bytecode; preserve flat legacy JWT and both transports; explicit official UI isolation; server-enforced sharing permissions; single instance.

- [x] Task 1: Official UI isolation. Add manifest JavaScript and Node tests loading the actual decorator. Verify two isolated stores cannot consume/revoke each other's token; normal mode delegates; unavailable sessionStorage uses memory. Run `node --test scripts/tab-isolation.test.mjs` before/after implementation.
- [x] Task 2: Java sharing. Extend authenticated configuration with validated GUAC_SHARE_ID/GUAC_JOIN_ID, expiry and read-only. Replace simple context connections with managed connections. Registry reserves unique host IDs before dialing; joins use guacd ID only. Test malformed claims, duplicate reservation, failed handshake cleanup, host close racing joins, expired join, and read-only instruction filtering. Run `./gradlew test` red then green.
- [x] Task 3: Broker and example. Add owner capabilities and five-minute invites, `/share` and `/join`; keep `/session` legacy response for callers not requesting sharing. Embed UI with independent windows, official UI entry, invitation controls and visitor route. Backend tests exercise real upstream HTTP fixture and expiry/ownership rejection. Run backend unit tests and frontend production build.
- [x] Task 4: Verification and release hold. Extend protocol integration for same-guacd sharing identity, input and close propagation. Run official UI and example browser two-tab flows, read-only/control visitor, record results, run Java matrix/build/backend/frontend checks, independent review, update docs. Do not claim release-ready without all gates.

## Progress and rulings

- Existing first-batch uncommitted changes retained; implementation uses feature branch in same workspace to avoid losing staged demo/build environment.
- Task boundaries: official frontend isolated from Java implementation; example broker/UI isolated from Java implementation using the approved claim contract.

- Verified local Java 8/17/21: 22 tests each; backend14, JavaScript11, real sharing and browser gates passed. Hosted CI and publishing are not part of completed local execution.
- Ruling: shared-host refresh requires new authorization — prevents old invitations from joining a new connection generation; transparent shared-host reconnection is intentionally unavailable.
- Review fixes: retired host IDs through grant expiry; explicit tunnel-to-Client close/error propagation in example.
